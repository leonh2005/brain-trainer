# 所有 .env 檔案（含子目錄），但允許 .env.example 範本
**/.env
**/.env.local
**/.env.production
!**/.env.example
.secrets/
# Python 虛擬環境：內含指向本機絕對路徑的 symlink，進 repo 會讓 Pages Jekyll
# build 在 CI 上找不到檔案而炸掉（2026-08-16 抓到 command-center/venv、roar-score/venv 誤入）
**/venv/
**/.venv/
tw-coverage/
BettaFish/
line-claude-bot/
telebot/

.playwright-mcp/
**/__pycache__/
*.pyc
# Added by code-review-graph
.code-review-graph/
timesfm/model/*.safetensors

# 截圖與圖片
*.png
*.jpg
*.jpeg
*.gif

# Log 檔案
*.log

# 壓縮檔
*.zip

# Superpowers brainstorm logs
.superpowers/
.claude/settings.local.json
scripts/hay-venv/
scripts/hay_current.json
scripts/hay_seen.json
sim-invest/sim.db
sim-invest/.coverage
shioaji-gateway/venv/

# Hermes 夜間訓練：內含 Steven 私人對話節錄，不可進公開 repo
hermes-training/tasks_*.json
hermes-training/scheduled_tasks_*.json
hermes-training/logs/

# 外部 clone（自帶 .git），誤 add 會被 git 記成沒對應 .gitmodules 的 submodule gitlink，
# 導致 GitHub Pages Jekyll build 每次 checkout 都 fail（2026-08-16 修過一次）
TradingAgents/
awesome-design-md/
daily-stock-analysis/
edible-plants-tw/
stock-screener-ai/
